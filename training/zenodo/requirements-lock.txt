# Pinned inference dependencies for the Pie tactic-prediction model.
# Exact versions captured from the environment that produced the published
# results (conda env `pie-train`, Python 3.11.15, RTX 4080, CUDA 12.1).
#
# torch is a CUDA 12.1 build served from the PyTorch index (not PyPI).
# Install with the extra index below (already declared in this file):
#
#     python -m pip install -r requirements-lock.txt
#
# Requires Python 3.11.x on Linux or Windows with an NVIDIA GPU.
# Run preflight.py first — it hard-fails on unsupported hardware.

--extra-index-url https://download.pytorch.org/whl/cu121

# ── Core model stack (4-bit LoRA inference) ──
torch==2.5.1+cu121
transformers==5.3.0
peft==0.18.1
bitsandbytes==0.49.2
accelerate==1.13.0
safetensors==0.7.0
tokenizers==0.22.2
huggingface_hub==1.7.2
hf-xet==1.4.2
numpy==2.3.5
sentencepiece==0.2.1
protobuf==7.34.1
sympy==1.13.1

# ── HTTP serving ──
fastapi==0.135.3
uvicorn==0.44.0
pydantic==2.12.5
pydantic_core==2.41.5
starlette==1.0.0
requests==2.32.5
